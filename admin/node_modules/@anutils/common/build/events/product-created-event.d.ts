import { Subject } from "./subjects";
export interface ProductCreatedEvent {
    subject: Subject.ProductCreated;
    data: {
        id: string;
        title: string;
        price: number;
    };
}
